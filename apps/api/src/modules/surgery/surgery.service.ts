import { AppError } from '../../shared/errors/app-error.js';
import type { AdmissionsConfigurationService } from '../admissions-configuration/admissions-configuration.service.js';
import type { BillingService } from '../billing/billing.service.js';
import type { DoctorRepository } from '../doctors/doctor.repository.js';
import type { Doctor } from '../doctors/doctor.types.js';
import type { PatientService } from '../patients/patient.service.js';
import type { OpdClinicalOrderService } from '../opd/opd-clinical-order.service.js';
import type { ClinicalOrderType, SaveOpdClinicalOrderDTO } from '../opd/opd-clinical-order.types.js';
import type { OpdPrescriptionService } from '../opd/opd-prescription.service.js';
import type { SaveOpdPrescriptionDTO } from '../opd/opd-prescription.types.js';
import type { ClinicalSourceContext } from '../opd/clinical-context.types.js';
import type { SurgeryRepository } from './surgery.repository.js';
import type { BookingLean, RecommendationLean } from './surgery.repository.js';
import type { ConfirmProcedureBookingDTO, CreateProcedureBookingDTO, CreateProcedureRecommendationDTO, ReasonDTO, RescheduleProcedureBookingDTO, SurgeryListQuery, SurgeryMetadata } from './surgery.types.js';
import type { SettingsRepository } from '../settings/settings.repository.js';
import { formatInTimeZone } from 'date-fns-tz';

const timeMinutes = (value: string) => { const [hour, minute] = value.split(':').map(Number); return (hour ?? 0) * 60 + (minute ?? 0); };
const duplicate = (error: unknown): never => { if (typeof error === 'object' && error !== null && 'code' in error && error.code === 11000) throw new AppError('An active recommendation or booking already exists', 409, 'PROCEDURE_DUPLICATE_CONFLICT'); throw error; };

import type { AdvancePaymentService } from '../advance-payment/advance-payment.service.js';

export class SurgeryService {
  constructor(private readonly repository: SurgeryRepository, private readonly doctors: DoctorRepository, private readonly patients: PatientService, private readonly billing: BillingService, private readonly beds: AdmissionsConfigurationService, private readonly advancePayment: AdvancePaymentService, private readonly prescriptions: OpdPrescriptionService, private readonly clinicalOrders: OpdClinicalOrderService, private readonly settingsRepository: SettingsRepository) {}
  private async authorize(actor: string, branchId: string) { if (!await this.repository.hasBranchAccess(actor, branchId)) throw new AppError('Branch access denied', 403, 'BRANCH_ACCESS_DENIED'); }
  private async authorizeDepartment(actor: string, departmentId: string) { const scope = await this.repository.departmentScope(actor); if (scope && !scope.includes(departmentId)) throw new AppError('Department access denied', 403, 'DEPARTMENT_ACCESS_DENIED'); }
  async listRecommendations(query: SurgeryListQuery, actor: string) { await this.authorize(actor, query.branch_id); return this.repository.listRecommendations(query, await this.repository.departmentScope(actor)); }
  async listBookings(query: SurgeryListQuery, actor: string) { await this.authorize(actor, query.branch_id); return this.repository.listBookings(query, await this.repository.departmentScope(actor)); }
  async getBooking(id: string, branchId: string, actor: string) { await this.authorize(actor, branchId); const row = await this.repository.getBooking(id, branchId); if (!row) throw new AppError('Procedure booking not found', 404, 'PROCEDURE_BOOKING_NOT_FOUND'); await this.authorizeDepartment(actor, row.department_id); return row; }

  async getPrescription(id: string, branchId: string, actor: string) { await this.requireActiveBookingContext(id, branchId, actor); return this.prescriptions.getForContext({ source_type: 'PROCEDURE_BOOKING', source_id: id }); }

  async submitPrescription(id: string, branchId: string, data: SaveOpdPrescriptionDTO, actor: string, metadata: SurgeryMetadata) { return this.submitDownstream(id, branchId, actor, metadata, async (context, session) => { const existing = await this.prescriptions.getForContext(context, session); const result = await this.prescriptions.submitForContext(context, data, actor, session); return { result, existing: Boolean(existing), kind: 'prescription' as const }; }); }

  async getClinicalOrder(id: string, branchId: string, orderType: ClinicalOrderType, actor: string) { await this.requireActiveBookingContext(id, branchId, actor); return this.clinicalOrders.getForContext({ source_type: 'PROCEDURE_BOOKING', source_id: id }, orderType); }

  async submitClinicalOrder(id: string, branchId: string, orderType: ClinicalOrderType, data: SaveOpdClinicalOrderDTO, actor: string, metadata: SurgeryMetadata) { return this.submitDownstream(id, branchId, actor, metadata, async (context, session) => { const existing = await this.clinicalOrders.getForContext(context, orderType, session); const result = await this.clinicalOrders.submitForContext(context, orderType, data, actor, session); return { result, existing: Boolean(existing), kind: orderType === 'LABORATORY' ? 'laboratory' as const : 'imaging' as const }; }); }

  async createRecommendation(data: CreateProcedureRecommendationDTO, actor: string, metadata: SurgeryMetadata) {
    await this.authorize(actor, data.branch_id); await this.authorizeDepartment(actor, data.department_id); const session = await this.repository.session();
    try { let result; await session.withTransaction(async () => { const refs = await this.repository.recommendationReferences(data, session); if (!refs.patient) throw new AppError('Active patient not found', 404, 'PATIENT_NOT_FOUND'); if (!refs.doctor) throw new AppError('Active recommending doctor not found in the selected department', 404, 'DOCTOR_NOT_FOUND'); if (!refs.department) throw new AppError('Active department not found in the selected branch', 404, 'DEPARTMENT_NOT_FOUND'); if (!refs.service) throw new AppError('Active procedure service not found in the selected department', 404, 'PROCEDURE_SERVICE_NOT_FOUND'); if (!refs.encounter) throw new AppError('Clinical encounter does not match the selected patient and doctor', 409, 'ENCOUNTER_CONTEXT_MISMATCH'); result = await this.repository.createRecommendation(data, { patientNumber: refs.patient.patientNumber, patientName: [refs.patient.firstName, refs.patient.middleName, refs.patient.lastName].filter(Boolean).join(' '), doctorName: refs.doctor.displayName, departmentName: refs.department.name, serviceName: refs.service.name }, actor, session); await this.patients.addProcedureTimeline(data.patient_id, 'PROCEDURE_RECOMMENDATION_CREATED', 'Procedure recommended', `${result.recommendation_number} recommends ${result.service_name}.`, actor, session); await this.repository.audit('surgery.recommendation.created', actor, metadata, { recommendationId: result.id, patientId: data.patient_id, encounterId: data.encounter_id, serviceId: data.service_id, branchId: data.branch_id }, session); }); if (!result) throw new AppError('Procedure recommendation could not be created', 500, 'PROCEDURE_RECOMMENDATION_CREATE_FAILED'); return result; } catch (error) { return duplicate(error); } finally { await session.endSession(); }
  }

  async cancelRecommendation(id: string, branchId: string, data: ReasonDTO, actor: string, metadata: SurgeryMetadata) { await this.authorize(actor, branchId); const session = await this.repository.session(); try { let result; await session.withTransaction(async () => { const current = await this.repository.getRecommendation(id, branchId, session); if (!current) throw new AppError('Procedure recommendation not found', 404, 'PROCEDURE_RECOMMENDATION_NOT_FOUND'); await this.authorizeDepartment(actor, current.department_id); result = await this.repository.cancelRecommendation(id, branchId, data.reason, actor, session); if (!result) throw new AppError('Only an unbooked active recommendation can be cancelled', 409, 'PROCEDURE_RECOMMENDATION_STATE_CONFLICT'); await this.patients.addProcedureTimeline(current.patient_id, 'PROCEDURE_RECOMMENDATION_CANCELLED', 'Procedure recommendation cancelled', `${current.recommendation_number} was cancelled: ${data.reason}`, actor, session); await this.repository.audit('surgery.recommendation.cancelled', actor, metadata, { recommendationId: id, patientId: current.patient_id, branchId, reason: data.reason }, session); }); return result; } finally { await session.endSession(); } }

  async createBooking(data: CreateProcedureBookingDTO, actor: string, metadata: SurgeryMetadata) { await this.authorize(actor, data.branch_id); const session = await this.repository.session(); try { let result; await session.withTransaction(async () => { const recommendation = await this.repository.getActiveRecommendationRecord(data.recommendation_id, data.branch_id, session); if (!recommendation) throw new AppError('Active unbooked procedure recommendation not found', 409, 'PROCEDURE_RECOMMENDATION_NOT_ACTIVE'); const refs = await this.repository.bookingReferences(recommendation, data.doctor_id, session); if (!refs.service || !refs.service.defaultDurationMinutes || !refs.service.bookingCapacity) throw new AppError('Procedure service configuration is incomplete', 409, 'PROCEDURE_SERVICE_CONFIGURATION_INVALID'); if (!refs.doctor) throw new AppError('Active procedure doctor not found in the selected branch and department', 404, 'DOCTOR_NOT_FOUND'); const start = new Date(data.scheduled_start); const end = new Date(start.getTime() + refs.service.defaultDurationMinutes * 60000); await this.repository.acquireConcurrencyLock(refs.doctor._id.toString(), refs.service._id.toString(), session); await this.validateSchedule(refs.doctor._id.toString(), start, end, refs.service._id.toString(), refs.service.bookingCapacity, undefined, session); result = await this.repository.createBooking(data, recommendation, { doctorName: refs.doctor.displayName, duration: refs.service.defaultDurationMinutes }, actor, session); const consumed = await this.repository.markRecommendationBooked(data.recommendation_id, result.id, actor, session); if (!consumed) throw new AppError('Recommendation was booked by another request', 409, 'PROCEDURE_RECOMMENDATION_STATE_CONFLICT'); await this.patients.addProcedureTimeline(recommendation.patientId.toString(), 'PROCEDURE_BOOKING_CREATED', 'Procedure booking created', `${result.booking_number} is pending confirmation for ${result.service_name}.`, actor, session); await this.repository.audit('surgery.booking.created', actor, metadata, { bookingId: result.id, recommendationId: data.recommendation_id, patientId: recommendation.patientId.toString(), branchId: data.branch_id, scheduledStart: start, scheduledEnd: end }, session); }); if (!result) throw new AppError('Procedure booking could not be created', 500, 'PROCEDURE_BOOKING_CREATE_FAILED'); return result; } catch (error) { return duplicate(error); } finally { await session.endSession(); } }

  async confirmBooking(id: string, branchId: string, data: ConfirmProcedureBookingDTO, actor: string, metadata: SurgeryMetadata) { await this.authorize(actor, branchId); const session = await this.repository.session(); try { let result; await session.withTransaction(async () => { const booking = await this.requireBookingRecord(id, branchId, actor, session, 'PENDING_CONFIRMATION'); const recommendation = await this.repository.getRecommendation(booking.recommendationId.toString(), branchId, session); const refs = await this.repository.bookingReferences(bookingRecommendation(booking), booking.doctorId.toString(), session); if (!recommendation || !refs.service || !refs.service.bookingCapacity) throw new AppError('Procedure booking context is no longer active', 409, 'PROCEDURE_BOOKING_CONTEXT_INVALID'); await this.repository.acquireConcurrencyLock(booking.doctorId.toString(), booking.serviceId.toString(), session); await this.validateSchedule(booking.doctorId.toString(), booking.scheduledStart, booking.scheduledEnd, booking.serviceId.toString(), refs.service.bookingCapacity, id, session); const holdId = data.hold_id ?? booking.holdId?.toString() ?? null; if (refs.service.requiresBed && (!holdId || !await this.repository.validateHold(holdId, booking.patientId.toString(), branchId, session))) throw new AppError('An active bed hold for this patient is required', 409, 'PROCEDURE_BED_HOLD_REQUIRED'); const consentId = data.consent_document_id ?? booking.consentDocumentId?.toString() ?? null; const consent = await this.patients.verifyContextConsent(booking.patientId.toString(), consentId, 'PROCEDURE_BOOKING', id, refs.service.requiresConsent, session); const requiredAmount = refs.service.minimumAdvanceDepositAmount ?? 0; const deposit = await this.advancePayment.syncRequirement({ source_type: 'PROCEDURE_BOOKING', source_id: id, patient_id: booking.patientId.toString(), branch_id: branchId, requirement_status: refs.service.requiresAdvanceDeposit ? 'REQUIRED' : 'NOT_REQUIRED', required_amount: refs.service.requiresAdvanceDeposit ? requiredAmount : 0 }, actor, session); if (deposit.requirement_status === 'REQUIRED' && deposit.payment_status !== 'PAID') throw new AppError(`An advance deposit of ${deposit.required_amount} is required before confirmation`, 409, 'ADVANCE_DEPOSIT_REQUIRED'); const snapshot = { consent_required: refs.service.requiresConsent, consent_satisfied: !refs.service.requiresConsent || Boolean(consent), consent_document_id: consent?.id ?? null, deposit_required: deposit.requirement_status === 'REQUIRED', deposit_satisfied: deposit.requirement_status !== 'REQUIRED' || deposit.payment_status === 'PAID', deposit_required_amount: deposit.required_amount, deposit_paid_amount: deposit.paid_amount, deposit_invoice_id: data.deposit_invoice_id ?? booking.depositInvoiceId?.toString() ?? null, deposit_payment_ids: [], bed_required: refs.service.requiresBed, bed_hold_id: holdId, verified_at: deposit.updated_at }; result = await this.repository.confirmBooking(id, branchId, { holdId, consentId, invoiceId: data.deposit_invoice_id ?? booking.depositInvoiceId?.toString() ?? null, snapshot }, actor, session); if (!result) throw new AppError('Booking changed before confirmation', 409, 'PROCEDURE_BOOKING_STATE_CONFLICT'); await this.patients.addProcedureTimeline(booking.patientId.toString(), 'PROCEDURE_BOOKING_CONFIRMED', 'Procedure booking confirmed', `${booking.bookingNumber} was confirmed for ${booking.scheduledStart.toISOString()}.`, actor, session); await this.repository.audit('surgery.booking.confirmed', actor, metadata, { bookingId: id, patientId: booking.patientId.toString(), branchId, prerequisiteSnapshot: snapshot }, session); }); return result; } finally { await session.endSession(); } }

  async rescheduleBooking(id: string, branchId: string, data: RescheduleProcedureBookingDTO, actor: string, metadata: SurgeryMetadata) { await this.authorize(actor, branchId); const session = await this.repository.session(); try { let result; await session.withTransaction(async () => { const booking = await this.requireBookingRecord(id, branchId, actor, session, 'BOOKED'); const doctorId = data.doctor_id ?? booking.doctorId.toString(); const refs = await this.repository.bookingReferences(bookingRecommendation(booking), doctorId, session); if (!refs.service || !refs.service.bookingCapacity || !refs.doctor) throw new AppError('Procedure booking context is no longer active', 409, 'PROCEDURE_BOOKING_CONTEXT_INVALID'); const start = new Date(data.scheduled_start); const end = new Date(start.getTime() + booking.durationMinutes * 60000); await this.repository.acquireConcurrencyLock(doctorId, booking.serviceId.toString(), session); await this.validateSchedule(doctorId, start, end, booking.serviceId.toString(), refs.service.bookingCapacity, id, session); const holdId = data.hold_id ?? booking.holdId?.toString() ?? null; if (refs.service.requiresBed && (!holdId || !await this.repository.validateHold(holdId, booking.patientId.toString(), branchId, session))) throw new AppError('An active bed hold for this patient is required', 409, 'PROCEDURE_BED_HOLD_REQUIRED'); await this.patients.verifyContextConsent(booking.patientId.toString(), data.consent_document_id ?? booking.consentDocumentId?.toString() ?? null, 'PROCEDURE_BOOKING', id, refs.service.requiresConsent, session); const deposit = await this.advancePayment.syncRequirement({ source_type: 'PROCEDURE_BOOKING', source_id: id, patient_id: booking.patientId.toString(), branch_id: branchId, requirement_status: refs.service.requiresAdvanceDeposit ? 'REQUIRED' : 'NOT_REQUIRED', required_amount: refs.service.requiresAdvanceDeposit ? (refs.service.minimumAdvanceDepositAmount ?? 0) : 0 }, actor, session); if (deposit.requirement_status === 'REQUIRED' && deposit.payment_status !== 'PAID') throw new AppError(`An advance deposit of ${deposit.required_amount} is required before rescheduling`, 409, 'ADVANCE_DEPOSIT_REQUIRED'); const oldHoldId = booking.holdId?.toString(); if (oldHoldId && oldHoldId !== holdId) await this.beds.releaseHoldSafe(oldHoldId, branchId, 'Surgery booking rescheduled', actor, metadata, session); result = await this.repository.rescheduleBooking(booking, start, end, doctorId, refs.doctor.displayName, data.reason, holdId, actor, session); if (!result) throw new AppError('Booking changed before reschedule', 409, 'PROCEDURE_BOOKING_STATE_CONFLICT'); await this.patients.addProcedureTimeline(booking.patientId.toString(), 'PROCEDURE_BOOKING_RESCHEDULED', 'Procedure booking rescheduled', `${booking.bookingNumber} was rescheduled: ${data.reason}`, actor, session); await this.repository.audit('surgery.booking.rescheduled', actor, metadata, { bookingId: id, patientId: booking.patientId.toString(), branchId, previousStart: booking.scheduledStart, newStart: start, previousDoctorId: booking.doctorId.toString(), newDoctorId: doctorId, reason: data.reason }, session); }); return result; } finally { await session.endSession(); } }

  async cancelBooking(id: string, branchId: string, data: ReasonDTO, actor: string, metadata: SurgeryMetadata) { await this.authorize(actor, branchId); const session = await this.repository.session(); try { let result; await session.withTransaction(async () => { const booking = await this.requireBookingRecord(id, branchId, actor, session, ['PENDING_CONFIRMATION', 'BOOKED']); if (booking.holdId) await this.beds.releaseHoldSafe(booking.holdId.toString(), branchId, data.reason, actor, metadata, session); result = await this.repository.cancelBooking(id, branchId, data.reason, actor, session); if (!result) throw new AppError('Booking changed before cancellation', 409, 'PROCEDURE_BOOKING_STATE_CONFLICT'); await this.patients.addProcedureTimeline(booking.patientId.toString(), 'PROCEDURE_BOOKING_CANCELLED', 'Procedure booking cancelled', `${booking.bookingNumber} was cancelled: ${data.reason}`, actor, session); await this.repository.audit('surgery.booking.cancelled', actor, metadata, { bookingId: id, patientId: booking.patientId.toString(), branchId, reason: data.reason, releasedHoldId: booking.holdId?.toString() ?? null }, session); }); return result; } finally { await session.endSession(); } }

  async completeBooking(id: string, branchId: string, actor: string, metadata: SurgeryMetadata) { await this.authorize(actor, branchId); const session = await this.repository.session(); try { let result; await session.withTransaction(async () => { const booking = await this.requireBookingRecord(id, branchId, actor, session, 'BOOKED'); if (booking.holdId) await this.beds.releaseHoldSafe(booking.holdId.toString(), branchId, 'Surgery completed', actor, metadata, session); result = await this.repository.completeBooking(id, branchId, actor, session); if (!result) throw new AppError('Only a started booked procedure can be completed', 409, 'PROCEDURE_BOOKING_NOT_COMPLETABLE'); await this.patients.addProcedureTimeline(booking.patientId.toString(), 'PROCEDURE_BOOKING_COMPLETED', 'Procedure booking completed', `${booking.bookingNumber} was marked completed.`, actor, session); await this.repository.audit('surgery.booking.completed', actor, metadata, { bookingId: id, patientId: booking.patientId.toString(), branchId }, session); }); return result; } finally { await session.endSession(); } }

  async alternatives(branchId: string, departmentId: string, serviceId: string, scheduledStart: string, actor: string) { await this.authorize(actor, branchId); const service = await this.repository.getProcedureService(serviceId); if (!service || service.departmentId.toString() !== departmentId || !service.defaultDurationMinutes || !service.bookingCapacity) throw new AppError('Active procedure service not found', 404, 'PROCEDURE_SERVICE_NOT_FOUND'); const start = new Date(scheduledStart); const end = new Date(start.getTime() + service.defaultDurationMinutes * 60000); const doctors = await this.doctors.list({ branch_id: branchId, department_id: departmentId, status: 'ACTIVE', page: 1, limit: 100 }); const available: Array<{ doctor_id: string; doctor_name: string }> = []; for (const doctor of doctors.data) { try { await this.validateSchedule(doctor.id, start, end, serviceId, service.bookingCapacity); available.push({ doctor_id: doctor.id, doctor_name: doctor.display_name }); } catch (error) { if (!(error instanceof AppError)) throw error; } } return available; }

  private async requireActiveBookingContext(id: string, branchId: string, actor: string) { await this.authorize(actor, branchId); const booking = await this.repository.getBooking(id, branchId); if (!booking) throw new AppError('Procedure booking not found', 404, 'PROCEDURE_BOOKING_NOT_FOUND'); await this.authorizeDepartment(actor, booking.department_id); if (!['PENDING_CONFIRMATION', 'BOOKED'].includes(booking.status)) throw new AppError('Procedure booking is not active', 409, 'PROCEDURE_CONTEXT_NOT_ACTIVE'); return booking; }

  private async submitDownstream<T extends { id: string }>(id: string, branchId: string, actor: string, metadata: SurgeryMetadata, submit: (context: ClinicalSourceContext, session: import('mongoose').ClientSession) => Promise<{ result: T; existing: boolean; kind: 'prescription' | 'laboratory' | 'imaging' }>) {
    await this.authorize(actor, branchId); const session = await this.repository.session();
    try { let output: T | undefined; await session.withTransaction(async () => {
      const booking = await this.repository.getBookingRecord(id, branchId, session);
      if (!booking) throw new AppError('Procedure booking not found', 404, 'PROCEDURE_BOOKING_NOT_FOUND');
      await this.authorizeDepartment(actor, booking.departmentId.toString());
      if (!['PENDING_CONFIRMATION', 'BOOKED'].includes(booking.status)) throw new AppError('Procedure booking is not active', 409, 'PROCEDURE_CONTEXT_NOT_ACTIVE');
      const recommendation = await this.repository.getRecommendation(booking.recommendationId.toString(), branchId, session);
      if (!recommendation || recommendation.patient_id !== booking.patientId.toString()) throw new AppError('Procedure recommendation context is invalid', 409, 'PROCEDURE_CONTEXT_INVALID');
      const context: ClinicalSourceContext = { source_type: 'PROCEDURE_BOOKING', source_id: id, encounter_id: recommendation.encounter_id, admission_id: null, procedure_id: id, patient_id: booking.patientId.toString(), patient_number: booking.patientNumber, patient_name: booking.patientName, doctor_id: booking.doctorId.toString(), doctor_name: booking.doctorName, branch_id: branchId };
      const submitted = await submit(context, session); output = submitted.result;
      if (!submitted.existing) { const label = submitted.kind; const event = submitted.kind === 'prescription' ? 'PROCEDURE_PRESCRIPTION_SUBMITTED' : submitted.kind === 'laboratory' ? 'PROCEDURE_LAB_ORDER_SUBMITTED' : 'PROCEDURE_IMAGING_ORDER_SUBMITTED'; await this.patients.addDownstreamTimeline(context.patient_id, event, `Procedure ${label} submitted`, `${label} submitted for ${booking.bookingNumber}.`, actor, session); await this.repository.audit(`surgery.downstream.${label}_submitted`, actor, metadata, { bookingId: id, patientId: context.patient_id, branchId, downstreamId: submitted.result.id }, session); }
    }); if (!output) throw new AppError('Downstream order could not be submitted', 500, 'PROCEDURE_DOWNSTREAM_SUBMIT_FAILED'); return output; } finally { await session.endSession(); }
  }

  private async validateSchedule(doctorId: string, start: Date, end: Date, serviceId: string, capacity: number, excludeId?: string, session?: import('mongoose').ClientSession) { if (!Number.isFinite(start.getTime()) || start <= new Date()) throw new AppError('Procedure start must be a valid future date and time', 400, 'INVALID_PROCEDURE_SCHEDULE'); if (start.toISOString().slice(0, 10) !== end.toISOString().slice(0, 10)) throw new AppError('Procedure cannot cross midnight', 400, 'INVALID_PROCEDURE_SCHEDULE'); const doctor = await this.doctors.getById(doctorId); if (!doctor || doctor.status !== 'ACTIVE') throw new AppError('Active doctor not found', 404, 'DOCTOR_NOT_FOUND'); const tz = (await this.settingsRepository.get()).localization.timezone; await this.validateDoctorAvailability(doctor, start, end, tz); if (await this.repository.hasAppointmentOverlap(doctorId, start, end, session)) throw new AppError('Doctor has an overlapping appointment', 409, 'DOCTOR_APPOINTMENT_CONFLICT'); if (await this.repository.hasDoctorOverlap(doctorId, start, end, excludeId, session)) throw new AppError('Doctor has an overlapping procedure booking', 409, 'DOCTOR_PROCEDURE_CONFLICT'); if (await this.repository.countServiceOverlap(serviceId, start, end, excludeId, session) >= capacity) throw new AppError('Procedure service capacity is full for the selected interval', 409, 'PROCEDURE_CAPACITY_CONFLICT'); }
  private async validateDoctorAvailability(doctor: Doctor, start: Date, end: Date, tz: string) { if (await this.doctors.hasActiveLeave(doctor.id, start)) throw new AppError('Doctor is on leave', 409, 'DOCTOR_ON_LEAVE'); const exception = await this.doctors.getExceptionByDate(doctor.id, start); const localDayOfWeek = formatInTimeZone(start, tz, 'EEEE').toUpperCase(); const recurring = doctor.availability.find((row) => row.day_of_week === localDayOfWeek); const availability = exception ? { is_available: exception.is_available, working_blocks: exception.working_blocks } : recurring; const startMinutes = timeMinutes(formatInTimeZone(start, tz, 'HH:mm')); const endMinutes = timeMinutes(formatInTimeZone(end, tz, 'HH:mm')); if (!availability?.is_available || !availability.working_blocks.some((block) => startMinutes >= timeMinutes(block.start_time) && endMinutes <= timeMinutes(block.end_time))) throw new AppError('Doctor is unavailable for the complete procedure interval', 409, 'DOCTOR_NOT_AVAILABLE'); }
  private async requireBookingRecord(id: string, branchId: string, actor: string, session: import('mongoose').ClientSession, status: string | string[]) { const booking = await this.repository.getBookingRecord(id, branchId, session); const statuses = Array.isArray(status) ? status : [status]; if (!booking || !statuses.includes(booking.status)) throw new AppError('Procedure booking is not in the required state', 409, 'PROCEDURE_BOOKING_STATE_CONFLICT'); await this.authorizeDepartment(actor, booking.departmentId.toString()); return booking; }
}

const bookingRecommendation = (booking: BookingLean): RecommendationLean => ({ _id: booking.recommendationId, recommendationNumber: '', patientId: booking.patientId, patientNumber: booking.patientNumber, patientName: booking.patientName, branchId: booking.branchId, departmentId: booking.departmentId, departmentName: booking.departmentName, recommendingDoctorId: booking.doctorId, recommendingDoctorName: booking.doctorName, serviceId: booking.serviceId, serviceName: booking.serviceName, encounterType: 'OPD_VISIT', encounterId: booking.recommendationId, clinicalReason: '', status: 'BOOKED', createdBy: booking.createdBy, updatedBy: booking.updatedBy, createdAt: booking.createdAt, updatedAt: booking.updatedAt });
