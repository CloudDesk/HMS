import { useState, type FormEvent } from 'react';
import type { ConsentContextType, ConsentTemplate, ConsentTemplateStatus } from '../api/consents';
import { Modal } from '../components/ui/Modal';
import { useConsentTemplatesFeature } from '../hooks/consents/useConsentTemplatesFeature';

type TemplateForm = { code: string; name: string; category: string; context_type: ConsentContextType; mandatory: boolean; status: ConsentTemplateStatus };
const empty: TemplateForm = { code: '', name: '', category: '', context_type: 'PATIENT', mandatory: false, status: 'ACTIVE' };
export function ConsentTemplatesPage() {
  const { state: { branches, branchId, templates, loading, saving }, capabilities, actions } = useConsentTemplatesFeature();
  const [open, setOpen] = useState(false); const [editing, setEditing] = useState<ConsentTemplate | null>(null); const [form, setForm] = useState<TemplateForm>(empty);
  const start = (item?: ConsentTemplate) => { setEditing(item ?? null); setForm(item ? { code: item.code, name: item.name, category: item.category, context_type: item.context_type, mandatory: item.mandatory, status: item.status } : empty); setOpen(true); };
  const submit = async (event: FormEvent) => { event.preventDefault(); await actions.save({ branch_id: branchId, ...form }, editing); setOpen(false); };
  return <><div className="appointment-page"><section className="appointment-page-header"><div className="appointment-page-title"><h2>Consent Templates</h2><p>Configure categories, mandatory triggers and versioned consent forms</p></div><div className="appointment-page-actions"><select aria-label="Branch" onChange={(e) => actions.setBranchId(e.target.value)} value={branchId}>{branches.map((branch) => <option key={branch.id} value={branch.id}>{branch.name}</option>)}</select><button className="doc-btn primary" disabled={!branchId || !capabilities.canCreate} onClick={() => start()} type="button">Add Template</button></div></section><section className="doc-card" style={{ padding: 0, overflow: 'hidden' }}><div className="table-responsive"><table className="data-table"><thead><tr><th>CODE</th><th>NAME</th><th>CATEGORY</th><th>CONTEXT</th><th>MANDATORY</th><th>VERSION</th><th>STATUS</th><th>ACTION</th></tr></thead><tbody>{loading ? <tr><td colSpan={8}>Loading...</td></tr> : templates.length === 0 ? <tr><td colSpan={8}>No consent templates configured.</td></tr> : templates.map((item) => <tr key={item.id}><td>{item.code}</td><td>{item.name}</td><td>{item.category}</td><td>{item.context_type}</td><td>{item.mandatory ? 'Yes' : 'No'}</td><td>v{item.version}</td><td><span className="doc-status active">{item.status}</span></td><td>{capabilities.canEdit ? <button className="doc-icon-action" onClick={() => start(item)} title="Edit" type="button"><i className="ph ph-pencil" /></button> : null}</td></tr>)}</tbody></table></div></section></div><Modal open={open} onClose={() => setOpen(false)} title={editing ? 'Edit Consent Template' : 'Add Consent Template'}><form className="modal-form" onSubmit={(e) => void submit(e)}><div className="doc-form-grid"><div className="doc-field"><label>Code</label><input required value={form.code} onChange={(e) => setForm({ ...form, code: e.target.value })} /></div><div className="doc-field"><label>Name</label><input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /></div><div className="doc-field"><label>Category</label><input required value={form.category} onChange={(e) => setForm({ ...form, category: e.target.value })} /></div><div className="doc-field"><label>Context</label><select value={form.context_type} onChange={(e) => setForm({ ...form, context_type: e.target.value as ConsentContextType })}><option value="PATIENT">Patient / EMR</option><option value="PROCEDURE">Procedure</option><option value="ADMISSION">Admission</option></select></div><div className="doc-field"><label>Status</label><select value={form.status} onChange={(e) => setForm({ ...form, status: e.target.value as 'ACTIVE' | 'INACTIVE' })}><option value="ACTIVE">Active</option><option value="INACTIVE">Inactive</option></select></div><label className="form-checkbox"><input checked={form.mandatory} onChange={(e) => setForm({ ...form, mandatory: e.target.checked })} type="checkbox" /> Mandatory before confirmation</label></div><div className="modal-actions"><button className="doc-btn" onClick={() => setOpen(false)} type="button">Cancel</button><button className="doc-btn primary" disabled={saving} type="submit">{saving ? 'Saving...' : 'Save'}</button></div></form></Modal></>;
}
