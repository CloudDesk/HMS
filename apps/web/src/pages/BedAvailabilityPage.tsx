import { useBedAvailabilityFeature } from '../hooks/reception/useBedAvailabilityFeature';
export function BedAvailabilityPage() {
  const { state, actions } = useBedAvailabilityFeature();
  return <div className="appointment-page"><section className="appointment-page-header"><div className="appointment-page-title"><h2>Bed Availability</h2><p>Read-only availability from Bed Management</p></div><div className="appointment-page-actions"><select aria-label="Branch" onChange={(e) => actions.setBranchId(e.target.value)} value={state.branchId}>{state.branches.map((branch) => <option key={branch.id} value={branch.id}>{branch.name}</option>)}</select></div></section><section className="consent-kpi-grid"><article className="doc-card consent-kpi-card"><div><span>Available</span><strong>{state.summary?.available ?? 0}</strong></div></article><article className="doc-card consent-kpi-card"><div><span>Occupied</span><strong>{state.summary?.occupied ?? 0}</strong></div></article><article className="doc-card consent-kpi-card"><div><span>Reserved</span><strong>{state.summary?.reserved ?? 0}</strong></div></article><article className="doc-card consent-kpi-card"><div><span>Unavailable</span><strong>{(state.summary?.blocked ?? 0) + (state.summary?.under_maintenance ?? 0)}</strong></div></article></section><section className="doc-card" style={{ padding: 0, overflow: 'hidden' }}><div className="table-responsive"><table className="data-table"><thead><tr><th>BED</th><th>WARD</th><th>ROOM</th><th>CATEGORY</th><th>STATUS</th></tr></thead><tbody>{state.loading ? <tr><td colSpan={5}>Loading available beds...</td></tr> : state.beds.length === 0 ? <tr><td colSpan={5}>No beds are currently available.</td></tr> : state.beds.map((bed) => <tr key={bed.id}><td><strong>{bed.bed_number}</strong></td><td>{bed.ward_name}</td><td>{bed.room_number ?? '-'}</td><td>{bed.bed_category}</td><td><span className="doc-status active">{bed.status}</span></td></tr>)}</tbody></table></div></section></div>;
}
